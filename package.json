{
  "name": "shoei-toso-website",
  "private": true,
  "scripts": {
    "extract-faq": "node scripts/extract-faq-data.js",
    "build-blog": "node scripts/build-blog.js",
    "build": "npm run extract-faq && npm run build-blog && npm run minify",
    "minify": "node scripts/minify.js",
    "preview": "npx --yes serve . -p 3000",
    "preview:dist": "npx --yes serve dist -p 3000"
  },
  "devDependencies": {
    "clean-css": "^5.3.0",
    "dotenv": "^16.4.5",
    "html-minifier-terser": "^7.2.0"
  }
}
